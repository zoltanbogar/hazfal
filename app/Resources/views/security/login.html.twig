{% extends "Admin/layout.html.twig" %}

{% block fos_user_content %}

    <div class="peers ai-s fxw-nw h-100vh">
        <div class="d-n@sm- peer peer-greed h-100 pos-r bgr-n bgpX-c bgpY-c bgsz-cv"
             style='background-image:  url("{{ asset('assets/images/bg.jpg') }}")'>
            {#<div class="pos-a centerXY">#}
            {#<div class="bgc-white bdrs-50p pos-r" style='width: 120px; height: 120px;'>#}
            {#<img class="pos-a centerXY" src="{{ asset('assets/images/sf.png') }}" alt="logo" />#}
            {#</div>#}
            {#</div>#}
        </div>

        <div class="col-12 col-md-4 peer pX-40 pY-80 h-100 bgc-white scrollable pos-r" style='min-width: 320px;'>
            {% if error %}
                <div class="alert alert-danger" role="alert">
                    {{ error.messageKey|trans(error.messageData, 'security') }}
                </div>
                <br/>
            {% endif %}

            <h3>Házfal Admin</h3>

            <h4 class="fw-300 c-grey-900 mB-40">Bejelentkezés</h4>
            <form action="{{ path('admin_post_loginn') }}" method="post">
                <div class="form-group">
                    <label class="text-normal text-dark">Username:</label>
                    <input type="text" class="form-control" placeholder="Felhasználónév"
                           id="username" name="_username" value="{{ last_username }}" required="required">
                </div>
                <div class="form-group">
                    <label class="text-normal text-dark">Password:</label>
                    <input type="password" class="form-control" placeholder="Jelszó"
                           id="password" name="_password" required="required">
                </div>
                <div class="form-group">
                    <div class="peers ai-c jc-sb fxw-nw">
                        <div class="peer">
                            <div class="checkbox checkbox-circle checkbox-info peers ai-c">
                                <input type="checkbox" id="inputCall1" name="inputCheckboxesCall" class="peer">
                                <label for="inputCall1" class=" peers peer-greed js-sb ai-c">
                                    <span class="peer peer-greed">Emlékezz rám</span>
                                </label>
                            </div>
                        </div>
                        <div class="peer">
                            <button class="btn btn-primary" id="_submit"
                                    name="_submit">Bejelentkezés
                            </button>
                        </div>
                    </div>
                </div>
            </form>
        </div>
    </div>

{% endblock fos_user_content %}
